name: client
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.1.10

  # UI
  cupertino_icons: ^1.0.8
  video_player: ^2.10.1
  chewie: ^1.13.0

  # Drift (SQLite local)
  drift: ^2.30.1
  drift_flutter: ^0.2.8   # <- versió compatible

  # Arxius i path
  path_provider: ^2.1.5
  path: ^1.9.1
  cached_network_image: ^3.3.1
  # HTTP
  http: ^1.1.0
  jwt_decoder: ^2.0.1
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  dio: ^5.9.1
  dio_cookie_manager: ^3.1.1  # or latest
  cookie_jar: ^4.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # Generació Drift
  drift_dev: ^2.30.1
  build_runner: ^2.10.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/
