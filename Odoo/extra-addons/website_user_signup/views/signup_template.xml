<odoo>
    <record id="menu_signup" model="website.menu">
            <field name="name">Signup</field>
            <field name="url">/signup</field>
            <field name="parent_id" ref="website.main_menu"/>
            <field name="sequence" type="int">52</field>
        </record>

    <template id="signup_template" name="Website Signup Page">
        <t t-call="website.layout">
            <div class="container my-5">
                <h2>Register</h2>
                <form action="/signup/submit" method="post" class="signup-form">
                    <div class="form-group">
                        <label>Name</label>
                        <input type="text" name="name" class="form-control" required = "required"/>
                    </div>
                    <div class="form-group">
                        <label>Email</label>
                        <input type="email" name="email" class="form-control" required= "required"/>
                    </div>
                    <div class="form-group">
                        <label>Password</label>
                        <input type="password" name="password" class="form-control" required= "required"/>
                    </div>
                    <button type="submit" class="btn btn-primary mt-2">Sign Up</button>
                </form>
            </div>
        </t>
    </template>
</odoo>
